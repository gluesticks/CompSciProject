package Subsystems.Sprites;

public class SEnemy
{
    protected void sprite()
    {
        //import the sprite from the folder
    }
    public void hitbox()
    {
        //determine how large the hitbox is
    }
    public boolean fire(boolean fire)
    {
        //hold a random time interval
        //if the time passed equals that interval 
            //fire bullet
        //else wait for a certain period
        return fire;
    }
    public void boundaryCheck(int enemyX, int enemyY)
    {
        //if eneny pos is outside the screen
            //bounce back into boundary
    }
}